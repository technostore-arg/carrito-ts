diff --git a/apps/futurohard/src/components/Header.jsx b/apps/futurohard/src/components/Header.jsx
index fe164ce..767320c 100644
--- a/apps/futurohard/src/components/Header.jsx
+++ b/apps/futurohard/src/components/Header.jsx
@@ -19,7 +19,7 @@ export default function Header({ search, onSearchChange, onSelectCategory, onSer
     <header className="header">
       <div className="header-inner">
         <button className="logo-link" onClick={() => onSelectCategory("todos")} aria-label="Futuro Hard inicio">
-          <img src={`${import.meta.env.BASE_URL}logo.png`} alt="Futuro Hard" style={{ height: 28, mixBlendMode: 'screen' }} />
+          <img src={`${import.meta.env.BASE_URL}logo.svg`} alt="Futuro Hard" style={{ height: 30 }} />
         </button>
 
         <nav className="nav nav--desktop">
diff --git a/apps/futurohard/src/index.css b/apps/futurohard/src/index.css
index a60db5f..e5d4e61 100644
--- a/apps/futurohard/src/index.css
+++ b/apps/futurohard/src/index.css
@@ -270,7 +270,8 @@ a { color: inherit; text-decoration: none; }
 .product-grid {
   display: grid;
   grid-template-columns: repeat(4, 1fr);
-  grid-auto-rows: 280px;
+  grid-auto-rows: minmax(280px, auto);
+  align-items: stretch;
   gap: 18px;
   padding-bottom: clamp(64px, 10vh, 110px);
 }
diff --git a/apps/technostore/src/index.css b/apps/technostore/src/index.css
index 1d1413f..d655434 100644
--- a/apps/technostore/src/index.css
+++ b/apps/technostore/src/index.css
@@ -18,7 +18,7 @@
   --amber: #fbbf24;
   --green: #10b981;
   --red: #ef4444;
-  --gradient-accent: linear-gradient(120deg, #FF5C00, #00D9C0);
+  --gradient-accent: linear-gradient(120deg, #FF5C00 0%, #FF3D8A 50%, #00D9C0 100%);
   --gradient-accent-soft: linear-gradient(120deg, rgba(255, 92, 0, 0.14), rgba(0, 217, 192, 0.14));
   --font-display: 'Space Grotesk', 'Inter', sans-serif;
   --radius: 20px;
@@ -270,7 +270,8 @@ a { color: inherit; text-decoration: none; }
 .product-grid {
   display: grid;
   grid-template-columns: repeat(4, 1fr);
-  grid-auto-rows: 280px;
+  grid-auto-rows: minmax(280px, auto);
+  align-items: stretch;
   gap: 18px;
   padding-bottom: clamp(64px, 10vh, 110px);
 }
diff --git a/server/ingesta/handler.js b/server/ingesta/handler.js
index a707a50..1bea9fb 100644
--- a/server/ingesta/handler.js
+++ b/server/ingesta/handler.js
@@ -90,7 +90,10 @@ function heuristicTextToRows(text) {
       const idx = line.indexOf(priceMatch[0])
       if (idx > 3) nombre = line.slice(0, idx).trim()
     }
-    nombre = nombre.replace(/^[\-\*\•\d\.\)\s]+/, '').trim().slice(0, 80)
+    nombre = nombre.replace(/^[\-\*\•\d\.\)\s]+/, '').trim()
+    // Sacar el indicador de moneda que queda pegado al final (ej. "... RAM – U$S", "... 128GB - USD")
+    nombre = nombre.replace(/[\-–—:]?\s*(U\s*\$\s*S|USD|ARS|\$)\s*$/i, '').trim()
+    nombre = nombre.slice(0, 80)
     if (nombre.length < 3) nombre = line.slice(0, 40)
 
     // Saltar líneas que son headers/separadores sin precio (ej. "LISTADO COMPLETO XIAOMI", "REDMI🔴", "---")
