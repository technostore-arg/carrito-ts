// Fallback vacío — los productos reales vienen del API (GET /api/products).
// Antes contenía 12 fakes (iPhone, MacBook, etc.) que confundían al ver el catálogo.
const raw = []

export const mockProducts = raw
export default mockProducts
